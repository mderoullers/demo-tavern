[pytest]
log_print = False
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
tavern-global-cfg = var.yaml
junit_suite_name = demo_test_suite
addopts =
    --junitxml=../target/test-results.xml
    --tb=short
    -p no:logging
    -vv
    -s
    --color=yes
